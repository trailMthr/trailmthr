/// MovementLedgerService (future)
/// Consent-first background-ish location history.
/// NOT tied to ActivityRecorder stats.
/// 
/// Goals:
/// - user-controlled retention + sampling cadence
/// - “paused activity shadow” optional hook (short pauses)
/// - supports Health AI insights (fatigue, consistency, mobility patterns)
class MovementLedgerService {
  // TODO: implement later
}
